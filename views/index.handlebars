
<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">Welcome To DevFlash News</h1>
    <p class="lead">Here we scrape our friends at <a href="https://www.developer-tech.com/">www.developer-tech.com</a> and allow our user to comment and like/dislike the articles. See an interesting headline? Check the comments to make sure its a good read!  </p>
  </div>
</div>



<div class="container">
    {{#each data}}
        <div class="row articleRow">
            <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                <div class="article">
                    <div class="articleHeader">
                        <h3 class="date">{{date}}</h3>
                        <hr>
                        <h1 class="headline">{{headline}}</h1>
                        <h3 class="author">{{author}}</h3>
                    </div>
                    <div class="articleBody">
                        <p class="summary">{{summary}}</p>
                        <a href="{{link}}">CLICK HERE FOR FULL STORY</a>
                    </div>
                    <div class="voteRow">
                        <button class="btn btn-info thumbsUp text-center thumbs" id="{{id}}"><i class="fas fa-thumbs-up"></i></button>
                        <button class="btn btn-info thumbsDown text-center thumbs" id="{{id}}"><i class="fas fa-thumbs-down"></i></button>
                    </div>
                    <p class="text-center">
                    <button class="btn btn-primary showComments" type="button" data-toggle="collapse" data-target="#collapse{{@index}}" data-article="{{id}}" aria-expanded="false" aria-controls="collapse{{@index}}">
                    Show Comments
                    </button>
                    </p>
                    <div class="collapse" id="collapse{{@index}}">
                    <div class="card card-body">
                        Comments Go Below Sir
                        <div class="comments{{id}}"></div>
                        <div class="postComment">                            
                            <input type="text" id="input${1/(\w+)/\u\1/g}" class="form-control {{id}}" placeholder="Post Comment Here">
                            <button type="button" class="btn btn-success addComment" data-article="{{id}}">Post Comment</button>
                        </div>
                    </div>
                    </div>
                </div>
            </div>
        </div>
    {{/each}}
</div>
